@model SelectList
@{
    Layout = "~/Views/Shared/_Layout.cshtml";
}

 @* 
     senderıd,
     accountıd,
     Amount

 *@
 <div class="mt-3">
<form action="SendMoney" method="post" class=" w-75 mx-auto">
    <input type="hidden" name="SenderId" value="@ViewBag.SenderId" />
   <div class="mb-3">
      <label> Göndericeğiniz Hesap Numarası</label>
        <select class="form-select" asp-items="@Model" name="AccountId">
        </select>
   </div>
   <div class="mb-3">
       <label> Gönderilecek Tutar</label>
        <input type="number" class="form-control" name="Amount" />
   </div>
   <div class="mb-3">
      <button type="submit" class="btn btn-primary" > Para Gönder  </button>
   </div>
       

    </form>
</div>